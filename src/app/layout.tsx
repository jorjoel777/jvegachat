import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";


const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "🧠 Jorge Vega ChatBot — AI-Powered Fullstack Portfolio with Firebase + OpenRouter + Next.js",
  description: "A full-stack chatbot portfolio built with Next.js 14, Firebase, and OpenRouter (GPT-4). This prototype demonstrates a personalized assistant capable of responding to service-related queries, collecting contact leads, and storing chat sessions in Firestore. Designed to be scalable, UX-friendly and ready for integration with clients.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/favicon.ico" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
